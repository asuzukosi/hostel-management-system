from django.apps import AppConfig

# This app manages the hostel activities and models of the project
class AppHostelConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_hostel'
